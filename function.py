#def get_formatted_name(first_name, last_name):
    #"""Return a full name, neatly formatted."""
   # full_name = f"{first_name}  {last_name}"
    #return full_name.title()
#full_name =get_formatted_name("abdallah", "suallah")
#print(full_name)


grid = [[0 for _ in range(-1, 2)] for _ in range(-1, 2)]
#for row in grid :
 #   for col in row:
  #      if grid[row][col] == 0:
   #         grid[row][col] = 1
    #    else:grid[row][col] = 0
print( grid, "\n")            

